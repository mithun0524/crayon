{
  "name": "crayon",
  "version": "0.3.0",
  "private": true,
  "description": "Crayon — autonomous AI coding agent",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter crayon-cli dev",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.14.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "node-pty"
    ],
    "overrides": {
      "jsondiffpatch": "^0.7.6",
      "diff": "^8.0.3",
      "ai": "^4.3.2"
    }
  },
  "devDependencies": {
    "@types/node": "^22.19.19"
  }
}
